// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "mysql"
}

enum PlatformRole {
  USER // Pengguna biasa platform
  SUPERADMIN // Admin tertinggi platform yang mengelola seluruh sistem
}

enum CompanyStatus {
  ACTIVE // Perusahaan aktif dan dapat menggunakan sistem
  SUSPENDED // Perusahaan dibekukan sementara oleh sistem/admin
  CANCELED // Perusahaan sudah membatalkan layanan
}

enum MembershipStatus {
  ACTIVE // Member bisa login dan bekerja normal
  SUSPENDED // Akses member dibekukan sementara karena alasan admin/compliance
  TERMINATED // Hubungan kerja/member sudah selesai permanen
}

enum SessionStatus {
  ACTIVE // Sesi login masih aktif dan dapat digunakan
  REVOKED // Sesi login dicabut, misalnya karena logout atau alasan keamanan
}

enum BillingInterval {
  MONTHLY // Tagihan berulang setiap bulan
  YEARLY // Tagihan berulang setiap tahun
}

enum SubscriptionStatus {
  TRIALING // Subscription sedang dalam masa trial
  ACTIVE // Subscription aktif dan dapat digunakan
  PAST_DUE // Pembayaran terlambat atau belum berhasil ditagih
  CANCELED // Subscription dibatalkan
  EXPIRED // Subscription sudah kedaluwarsa
}

enum AuthProvider {
  GOOGLE // Login menggunakan akun Google
}

model User {
  id                   String       @id @default(uuid(7)) @map("userId")
  email                String?      @unique @db.VarChar(191)
  name                 String       @db.VarChar(191)
  passwordHash         String       @db.VarChar(255)
  passwordLoginEnabled Boolean      @default(true)
  mustChangePassword   Boolean      @default(true)
  passwordChangedAt    DateTime?
  platformRole         PlatformRole @default(USER)
  isActive             Boolean      @default(true)
  createdAt            DateTime     @default(now())
  updatedAt            DateTime     @updatedAt

  memberships           Membership[]
  terminatedMemberships Membership[]       @relation("MembershipTerminatedBy")
  sessions              UserSession[]
  authProviders         UserAuthProvider[]
  createdCompanies      Company[]          @relation("CreatedByUser")

  @@map("users")
}

model UserAuthProvider {
  id                 String       @id @default(uuid(7)) @map("userAuthProviderId")
  userId             String
  provider           AuthProvider
  providerAccountId  String       @db.VarChar(191)
  email              String?      @db.VarChar(191)
  emailVerified      Boolean      @default(false)
  displayName        String?      @db.VarChar(191)
  avatarUrl          String?      @db.VarChar(500)
  lastLoginAt        DateTime?
  createdAt          DateTime     @default(now())
  updatedAt          DateTime     @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@unique([provider, providerAccountId])
  @@index([userId])
  @@index([provider, email])
  @@map("user_auth_providers")
}

model Company {
  id        String @id @default(uuid(7)) @map("companyId")
  name      String @db.VarChar(191)

  email        String? @db.VarChar(191)
  phone        String? @db.VarChar(50)
  logoUrl      String? @db.VarChar(500)
  addressLine1 String? @db.VarChar(255)
  city         String? @db.VarChar(100)
  province     String? @db.VarChar(100)
  timezone     String? @db.VarChar(50)

  status          CompanyStatus @default(ACTIVE)
  createdByUserId String?
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
  deletedAt       DateTime?

  createdByUser User?          @relation("CreatedByUser", fields: [createdByUserId], references: [id], onDelete: SetNull, onUpdate: Cascade)
  memberships   Membership[]
  roles         CompanyRole[]
  subscriptions Subscription[]
  departments   Department[]
  positions     Position[]
  locations     Location[]

  @@index([status])
  @@index([deletedAt])
  @@map("companies")
}

model Membership {
  id           String           @id @default(uuid(7)) @map("membershipId")
  companyId    String
  userId       String
  status       MembershipStatus @default(ACTIVE)
  isOwner      Boolean          @default(false)
  joinedAt     DateTime         @default(now())
  lastLoginAt  DateTime?

  employmentEndedAt  DateTime?
  terminationType    String?   @db.VarChar(50)
  terminationReason  String?   @db.Text
  terminatedByUserId String?
  accessRevokedAt    DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  company             Company              @relation(fields: [companyId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  user                User                 @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  terminatedBy        User?                @relation("MembershipTerminatedBy", fields: [terminatedByUserId], references: [id], onDelete: SetNull, onUpdate: Cascade)
  roles               MembershipRole[]
  sessions            UserSession[]
  employeeProfile     EmployeeProfile?
  employeeDepartments EmployeeDepartment[]
  employeePositions   EmployeePosition[]

  @@unique([companyId, userId])
  @@index([companyId, status])
  @@index([userId, status])
  @@index([terminatedByUserId])
  @@map("memberships")
}

model Permission {
  id           String   @id @default(uuid(7)) @map("permissionId")
  module       String   @db.VarChar(100)
  action       String   @db.VarChar(100)
  name         String   @db.VarChar(191)
  description  String?  @db.Text
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  rolePermissions CompanyRolePermission[]

  @@unique([module, action])
  @@index([module])
  @@map("permissions")
}

model CompanyRole {
  id            String   @id @default(uuid(7)) @map("companyRoleId")
  companyId     String
  name          String   @db.VarChar(191)
  description   String?  @db.Text
  isSystem      Boolean  @default(false)
  isDefault     Boolean  @default(false)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  company         Company                 @relation(fields: [companyId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  rolePermissions CompanyRolePermission[]
  membershipRoles MembershipRole[]

  @@unique([companyId, name])
  @@index([companyId, isDefault])
  @@map("company_roles")
}

model CompanyRolePermission {
  id                      String   @id @default(uuid(7)) @map("companyRolePermissionId")
  roleId                  String
  permissionId            String
  createdAt               DateTime @default(now())

  role       CompanyRole @relation(fields: [roleId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  permission Permission  @relation(fields: [permissionId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@unique([roleId, permissionId])
  @@index([permissionId])
  @@map("company_role_permissions")
}

model MembershipRole {
  id               String   @id @default(uuid(7)) @map("membershipRoleId")
  membershipId     String
  roleId           String
  createdAt        DateTime @default(now())

  membership Membership  @relation(fields: [membershipId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  role       CompanyRole @relation(fields: [roleId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@unique([membershipId, roleId])
  @@index([roleId])
  @@map("membership_roles")
}

model SubscriptionPlan {
  id                 String          @id @default(uuid(7)) @map("subscriptionPlanId")
  name               String          @db.VarChar(191)
  description        String?         @db.Text
  pricePerUser       Decimal         @db.Decimal(15, 2)
  currency           String          @default("IDR") @db.VarChar(10)
  interval           BillingInterval
  isActive           Boolean         @default(true)
  createdAt          DateTime        @default(now())
  updatedAt          DateTime        @updatedAt

  subscriptions Subscription[]

  @@unique([name, interval])
  @@map("subscription_plans")
}

model Subscription {
  id                   String             @id @default(uuid(7)) @map("subscriptionId")
  companyId            String
  planId               String
  status               SubscriptionStatus @default(TRIALING)
  seatLimit            Int
  pricePerUserSnapshot Decimal            @db.Decimal(15, 2)
  currencySnapshot     String             @default("IDR") @db.VarChar(10)
  startsAt             DateTime           @default(now())
  trialEndsAt          DateTime?
  currentPeriodStart   DateTime?
  currentPeriodEnd     DateTime?
  canceledAt           DateTime?
  createdAt            DateTime           @default(now())
  updatedAt            DateTime           @updatedAt

  company Company          @relation(fields: [companyId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  plan    SubscriptionPlan @relation(fields: [planId], references: [id], onDelete: Restrict, onUpdate: Cascade)

  @@index([companyId, status])
  @@index([planId])
  @@map("subscriptions")
}

model UserSession {
  id               String        @id @default(uuid(7)) @map("userSessionId")
  userId           String
  membershipId     String?
  refreshTokenHash String        @unique @db.Char(64)
  deviceId         String?       @db.VarChar(191)
  deviceName       String?       @db.VarChar(100)
  platform         String?       @db.VarChar(50)
  ipAddress        String?       @db.VarChar(64)
  userAgent        String?       @db.VarChar(512)
  status           SessionStatus @default(ACTIVE)
  lastUsedAt       DateTime?
  renewedAt        DateTime?
  expiresAt        DateTime
  revokedAt        DateTime?
  revokedReason    String?       @db.VarChar(255)
  createdAt        DateTime      @default(now())
  updatedAt        DateTime      @updatedAt

  user       User        @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  membership Membership? @relation(fields: [membershipId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@index([userId, status])
  @@index([membershipId, status])
  @@index([status, expiresAt])
  @@map("user_sessions")
}

model EmployeeProfile {
  id                String @id @default(uuid(7)) @map("employeeProfileId")
  membershipId      String @unique

  employeeNumber        String? @db.VarChar(100)
  phone                 String? @db.VarChar(50)
  emergencyContactName  String? @db.VarChar(191)
  emergencyContactPhone String? @db.VarChar(50)

  birthDate     DateTime? @db.Date
  birthPlace    String?   @db.VarChar(191)
  gender        String?   @db.VarChar(20)
  maritalStatus String?   @db.VarChar(50)

  addressLine1 String? @db.VarChar(255)
  city         String? @db.VarChar(100)
  province     String? @db.VarChar(100)

  employmentType   String?   @db.VarChar(50)
  hireDate         DateTime? @db.Date
  probationEndDate DateTime? @db.Date

  photoUrl String? @db.VarChar(500)
  notes    String? @db.Text

  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  deletedAt DateTime?

  membership Membership @relation(fields: [membershipId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@index([employeeNumber])
  @@index([hireDate])
  @@index([deletedAt])
  @@map("employee_profiles")
}

model Department {
  id           String    @id @default(uuid(7)) @map("departmentId")
  companyId    String
  name         String    @db.VarChar(191)
  isActive     Boolean   @default(true)
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  deletedAt    DateTime?

  company       Company              @relation(fields: [companyId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  employeeLinks EmployeeDepartment[]

  @@unique([companyId, name])
  @@index([companyId, isActive])
  @@index([deletedAt])
  @@map("departments")
}

model Position {
  id         String    @id @default(uuid(7)) @map("positionId")
  companyId  String
  name       String    @db.VarChar(191)
  isActive   Boolean   @default(true)
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt
  deletedAt  DateTime?

  company       Company            @relation(fields: [companyId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  employeeLinks EmployeePosition[]

  @@unique([companyId, name])
  @@index([companyId, isActive])
  @@index([deletedAt])
  @@map("positions")
}

model Location {
  id         String    @id @default(uuid(7)) @map("locationId")
  companyId  String
  name       String    @db.VarChar(191)
  latitude   Decimal?  @db.Decimal(10, 6)
  longitude  Decimal?  @db.Decimal(10, 6)
  isActive   Boolean   @default(true)
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt
  deletedAt  DateTime?

  company Company @relation(fields: [companyId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@unique([companyId, name])
  @@index([companyId, isActive])
  @@index([deletedAt])
  @@map("locations")
}

model EmployeeDepartment {
  id                   String   @id @default(uuid(7)) @map("employeeDepartmentId")
  membershipId         String
  departmentId         String
  isPrimary            Boolean  @default(true)
  createdAt            DateTime @default(now())

  membership Membership @relation(fields: [membershipId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  department Department @relation(fields: [departmentId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@unique([membershipId, departmentId])
  @@index([departmentId])
  @@map("employee_departments")
}

model EmployeePosition {
  id                 String   @id @default(uuid(7)) @map("employeePositionId")
  membershipId       String
  positionId         String
  isPrimary          Boolean  @default(true)
  createdAt          DateTime @default(now())

  membership Membership @relation(fields: [membershipId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  position   Position   @relation(fields: [positionId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@unique([membershipId, positionId])
  @@index([positionId])
  @@map("employee_positions")
}
